---
import Item from '~/components/widgets/Card.astro';
import type { Card } from '~/types';

const { cards } = Astro.props;
---

  <ul class="grid md:grid-cols-4 gap-10">
    {
      cards.map((card) => (
        <li class="mb-12 md:mb-10">
          <Item card={card}/>
        </li>
      ))
    }
  </ul>
